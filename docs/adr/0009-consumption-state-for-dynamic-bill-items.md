# Consumption state for dynamic bill items

动态账单 will keep bill items after the user opens or consumes them, but the default view will emphasize unconsumed items. Opening a video from the bill records intent, while actual consumption should be confirmed through watch history or player events before the item is treated as consumed.
