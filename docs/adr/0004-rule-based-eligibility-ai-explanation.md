# Rule-based eligibility with AI explanation

动态账单 will use deterministic local rules to decide whether a followed video submission enters a 账单栏目, while AI is used for summaries and human-readable explanations. This keeps the product from becoming an opaque recommendation feed and makes each bill item auditable against Bili-Bill's local history, follow relationship, and recent-consumption signals.
