# Brand shell refresh with dynamic bill

The 动态账单 release will include a Bili-Bill brand shell refresh so the new page does not feel bolted onto the old dark analytics dashboard. The same version should align naming, navigation, color, cards, buttons, states, and typography across the Dashboard shell and legacy pages, while deeper redesigns of legacy analytics pages can remain a later project.
