# Default retention becomes 180 days for dynamic bill

Bili-Bill will move the default watch-history retention target from 90 days to 180 days so 动态账单 can reason over durable interests. Existing users should not have their configured retention silently overwritten, and the UI should explain when less than 180 days of local history is available.
