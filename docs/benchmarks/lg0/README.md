# LG-0 有限规模合成验证

关联 #270。隔离原型，未接入生产扩展。日期 2026-09-06。

## 当前裁决

**产物验证 pass；性能候选 fail；LG-0 未完成，不能开始 LG-1。**

正式历史基线是干净提交 `3040206b980477f3a9cc989b9f2117901226b513` 的 [第三轮 48-run 收据](2026-09-06T08-40-01-622Z-324b51e8/report.json)、[测前记录](2026-09-06T08-40-01-622Z-324b51e8/preflight.json)及 [裁决](2026-09-06T08-40-01-622Z-324b51e8/verdict.json)。CLI 以该明确祖先提交验证历史 blob，不把旧收据绑定到后续优化源码。前两轮保留为历史诊断，三轮合计 144 次，不覆盖失败、不跨轮挑最好值。

第三轮搜索各场景最大 cold p95=231.7ms、warm p95=196.6ms，均符合候选。10 MiB 多条/单条场景最长主线程任务分别 764/852ms，阶段采样增长分别约 318.2/659.1 MiB；引用密集场景最长任务 365ms。因此仍有 3 个主线程超额、2 个采样增长超额。没有达到 200ms 和 256 MiB 候选，且运行中取消、真实中断等证据未补齐。

第三轮在测量前验证提交干净并比对全部源码 blob；伪造副本经写入或导出均拒绝。全部 48 次运行及故障收据有效，无运行错误，新建合成 profile 已清理。单机未做 CPU/操作系统调度隔离，采样非绝对峰值，不能将这些实验结果推广为所有设备的性能或完整产品体验。

## Worker 定向优化（六次，不是第四轮完整矩阵）

用户确认减少重复测试后，仅复测原失败的三个场景，每个首次恢复与幂等恢复各一次。[定向收据](worker-2026-09-06T09-28-13-067Z-f687f5fe/report.json)绑定测量时源码和实际两个 bundle，基线 HEAD 为 `2fcf8e3`，当时源码为明确标记的 working-tree-snapshot。源码规范 LF 哈希已核对；不宣称这是一份干净提交正式矩阵。

本次改动：原生 JSON 编码排序键、逐记录计算逻辑字节、减少重复克隆、CAS 事务内差分写入和无变化零写入；导入/导出/检索移入独立实验 Worker。文件和导出通过 Blob 传递，恢复返回数量摘要，不把整个资产数组发回主线程。

| 场景 | 首次恢复 ms | 幂等恢复 ms | 两次最大合计采样堆增长 MiB | 最长观测主线程任务 |
| --- | ---: | ---: | ---: | --- |
| 1000 条合计 10 MiB | 344.0 | 471.7 | 111.9 | 无 >=50ms 长任务 |
| 单条 10 MiB | 266.5 | 416.4 | 180.1 | 无 >=50ms 长任务 |
| 1000 条引用密集 | 246.4 | 274.1 | 16.6 | 无 >=50ms 长任务 |

主线程心跳最大间隔 32.1ms。合计采样堆值最高 200.7 MiB，增长最高 180.1 MiB，**包含主线程和 Worker 的 JS heap，不是只看主线程**；未强制 GC，采样不等于绝对峰值或 RSS。首次/幂等的起始堆与 GC 时机不同，幂等仍需读库、校验、比较，不承诺总耗时一定较短。运行流程与历史多阶段演练不同，不计算同比百分比、不以单次测量推断 p95。

全部六次完整导出哈希一致、容量/数量和检索结果一致；幂等恢复未改完整状态或 revision。提交前取消无写入，prepared 阶段终止 Worker 后重连无部分写入，旧 epoch 拒绝，提交开始后迟到取消仍报告实际提交成功。该终止测试不覆盖真实扩展或进程崩溃。新 profile 已清理。

定向功能检查 pass；正式 gate 未评估，LG-1 仍不解锁。本轮 focused 23/23、typecheck/build 通过；没有重跑本地全量、48-run、B1 或 audit。后续按相关失败点迭代，方案稳定后一次完整验收，继续保留现有容量与性能阈值。

本轮由 gpt-5.6-terra / xhigh 做一次范围内只读中立复核，未发现明确 P0/P1/P2 数据丢失、CAS/clear/cancel 错误或测量误报；确认源码绑定一致。此结论只支持保留在 Draft 的实验增量，不替代正式性能、生产 UI 或发布验收。

## 第二轮诊断

修正中间版为 [第二轮 48-run 收据](2026-09-06T08-31-42-524Z-77f22d7e/report.json)及其 [verdict](2026-09-06T08-31-42-524Z-77f22d7e/verdict.json)。测量结束时的逐文件绑定与 bundle 一致，但收据 commit 是测量前 HEAD，不包含当时未提交源码；因此只作为诊断，完整中间源码保存在 `7764ca4`。首轮基线保存在提交 `c813ea4d264b5b5731dea72e5f5d80165d17ee35`，同样只作为历史诊断，不能冒充当前代码的正式证据。

第二轮修复了中立复核发现的并发恢复覆盖风险、导入副本身份伪造、成功保存后的确认重试，以及新测试目录清理和 runner 故障收据核验。未作性能优化或放宽候选：搜索最大 cold p95 为 173.1ms、warm p95 为 198.3ms；最长主线程任务仍有 776ms，单条 10 MiB 场景阶段采样堆增长达到 690.0 MiB。三个场景超过主线程候选，两个超过 256 MiB 采样增长候选。

第二轮全部运行无异常，18 个新建合成 profile 已在 runner 完成后清理。首轮遗留的 18 个合成 profile 的 PowerShell 清理请求被平台策略拒绝；没有换工具绕过，也没有读取或提交其内容，仍位于本地忽略的 release-artifacts/lg0 首轮目录。该清理残留需单独处理，不将其伪装成已清理。

## 首轮记录

首轮原始收据在 [2026-09-06T08-18-46-671Z-a891606c](2026-09-06T08-18-46-671Z-a891606c/report.json)，测前冻结记录在 [preflight.json](2026-09-06T08-18-46-671Z-a891606c/preflight.json)，完整裁决在 [verdict.json](2026-09-06T08-18-46-671Z-a891606c/verdict.json)。runner 的 lg0GateStatus 是未裁决占位，最终以 verifier 的 candidateGateStatus 为准；不得用占位值掩盖已测出的失败。

6 个固定场景，每个 3 cold + 5 warm，共 48 次，无运行错误、无页面外部请求。真实 Chrome 152.0.7977.77（已安装 stable）、Node v24.14.1。官方 2026-09-04 的 [stable 元数据](https://googlechromelabs.github.io/chrome-for-testing/last-known-good-versions.json) 为 152.0.7977.82；本轮未自动升级浏览器，不声称验证最新 patch。精确 OS/CPU/内存见收据。

| 场景 | 冷搜索 p95 ms | 暖搜索 p95 ms | 最长主线程任务 ms | 最大阶段采样堆增长 MiB |
| --- | ---: | ---: | ---: | ---: |
| 空库 | 113.2 | 6.7 | 0 | 0.8 |
| 30 条典型 | 126.4 | 6.8 | 0 | 1.7 |
| 1000 条数量边界 | 465.7 | 10.8 | 59 | 27.4 |
| 1000 条合计 10 MiB | 198.8 | 205.2 | 767 | 372.7 |
| 单条 10 MiB | 70.2 | 62.5 | 781 | 500.0 |
| 1000 条引用密集 | 78.3 | 41.0 | 277 | 76.9 |

搜索均达到 cold <=2000ms / warm <=500ms 候选。后三场景主线程任务超过 200ms；两个 10 MiB 场景的阶段采样增长超过预先声明的 256 MiB 候选。采样不是绝对峰值，但已观察到的超额足以证明该方案未达到该候选，不需要将未测到的峰值当作通过。

全部 48 次同一运行内往返提交回读一致；18 次播种后的正常浏览器关闭/重开保留数量与规范字节大小（未记录跨进程完整内容哈希，不能据此声称逐字节重启一致性已验收），21 个真实 v13 schema 的合成表均保持内容不变。注入升级失败、事务 abort/quota、提交前取消与清空后的旧 epoch 写入均被拒绝且不损害已有记录。以上不证明真实磁盘配额耗尽、强制杀进程或扩展 service worker 被终止后的行为。

## 解释与最小修复方向

证据支持**当前主线程全量 JSON 预检/恢复原型存在计算与分配开销**，不支持“10 MiB 产品范围必须放弃”或“浏览器环境是唯一原因”。当前实现同时持有原记录、JSON 字符串、UTF-8 缓冲、解码结果、冲突副本与事务克隆；阶段时延与高水位定位了风险，但未做逐函数内存归因，不能把全部增长都归因于某一个函数。

Worker 与减少复制/重复写入的第一步已完成上述定向验证。后续保持 1000/10 MiB，收敛剩余中断、恶意输入内存与生产接入边界，再安排稳定方案的一次完整验收。Worker 只能隔离主线程，不能单独证明内存问题解决。需要降低容量或改变时间目标时另行提出范围修订，不能静默改。

## 历史基线验证与边界

- `node --test tests/lg0*.test.ts`：数据合同与报告验证器回归 20/20（包含第三轮来源绑定回归）。
- `node scripts/lg0/verify-report.mjs --verify docs/benchmarks/lg0/2026-09-06T08-40-01-622Z-324b51e8 3040206b980477f3a9cc989b9f2117901226b513`：检查场景、次数、计时、容量、故障及历史干净提交绑定；验证器 PASS 不等于候选通过。优化后必须指定历史提交，不能默认核验当前源码。
- 验证历史首轮时使用 `node scripts/lg0/verify-report.mjs --verify docs/benchmarks/lg0/2026-09-06T08-18-46-671Z-a891606c c813ea4d264b5b5731dea72e5f5d80165d17ee35`，只检查明确的祖先提交，不把旧结果绑定到修正版。
- 全量测试 628/629；唯一失败仍是现有 CI 文件 CRLF 与 LF-only 正则不兼容（tests/gate-014-b1-matrix-runner.test.ts:99），未改该文件或测试。
- typecheck / build 通过；release distribution 18 notices、8 entries、14 chunks；npm audit 0 vulnerabilities。
- 历史 B1 verify 仍 pass，4680 operations，绑定未改；没有重跑或替换旧 B1。
- 未读 Cookie、用户 profile、登录状态、凭据或用户库，未采集字幕、调用 AI、修改 release/tag/package。

新脚本及这份报告不是生产实现，候选数据合同还需审查与补齐文档所列缺口。PR 保持 Draft，不将 #270 或 0.14.0 标记完成。

中立 gpt-5.6-terra / xhigh 双轴复核：并发恢复、成功保存确认、导入身份、干净提交溯源及来源认证表述的问题均已修复并再次复核；当前增量无未解决 P0/P1/P2，可作为未放行的 Draft 实验交付。代码复核不等于性能候选通过，也不解锁 LG-1。
